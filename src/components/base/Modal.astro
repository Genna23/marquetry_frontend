---
import Modal from "../../partials/base/Modal.astro";
import ModalButton from "../../partials/base/ModalButton.astro";

const { blok, classes } = Astro.props;

const { label, style, icon, title, content } = blok;

const id = 'modal_' + blok._uid

---

<ModalButton {id} {style} {icon} {classes}>{label}</ModalButton>
<Modal {id} {title} {content}/>
