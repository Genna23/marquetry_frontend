---

import Button from '../../partials/base/Button.astro'

const {blok, classes} = Astro.props

const {label, style, link, icon} = blok

---

<Button {style} href={link} {icon} {classes}>{label}</Button>