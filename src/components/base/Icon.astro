---

import Icon from '../../partials/base/Icon.astro'

const {blok, classes, color} = Astro.props

const {url, icon, style} = blok

const icon_name = icon

---

<Icon {style} href={url.cashed_url} icon={icon_name} {classes}/>