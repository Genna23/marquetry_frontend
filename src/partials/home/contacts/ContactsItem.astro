---

import { Icon } from "astro-icon/components";

const { title, icon } = Astro.props

---

<div class="flex gap-4 items-center">
    <Icon name={icon} class="text-[2rem]" />
    <span>{title}</span>
</div>
